import { DragDropDirectiveTs } from './drag-drop.directive.ts';

describe('DragDropDirectiveTs', () => {
  it('should create an instance', () => {
    const directive = new DragDropDirectiveTs();
    expect(directive).toBeTruthy();
  });
});
